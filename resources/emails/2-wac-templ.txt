Standard fields:

BlockNumber is $BlockNumber$
BlockTimestamp is $BlockTimestamp$

The matched value is $MatchedValue$

All values returned from contract call: $AllValues$

The first element is: !AllValues[0]
The second element is: !AllValues[1]
This does not exist: !AllValues[333]
